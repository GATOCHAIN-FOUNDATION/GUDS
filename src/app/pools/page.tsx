import React from 'react';
import MobileLayout from '@/components/layout/mobile-layout';
import PoolsContainer from '@/components/pools/pools-container';
const Pools = () => {
  return (
    <MobileLayout>
      <PoolsContainer />
    </MobileLayout>
  );
};

export default Pools;
